{
  "name": "node-config-template",
  "description": "A template for node config.",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "commitlint.enable": "husky install"
  },
  "devDependencies": {
    "@commitlint/cli": "^17.0.3",
    "@commitlint/config-conventional": "^17.0.3",
    "husky": "^8.0.1"
  }
}
